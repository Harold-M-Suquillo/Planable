import { createTheme, ThemeProvider, styled } from '@mui/material/styles'
const theme = createTheme({
    palette: {
        primary: {
            main: '#005070'
        },
        secondary: {
            main: '#a92717'
        }
    }

});

export { theme }